<%= form_tag root_path,
  class: "simple_form search d-flex mt-0 mr-2 inputPgSearch",
  label: "Search",
  method: :get,
  data: { search_bullets_target: 'form', event_list_target:'inputSearch' } do %>

  <%= text_field_tag :query,
    params[:query],
    label: "Search",
    class: "form-control string required",
    placeholder: "type and press 'Enter'",
    autocomplete: "off",
    data: { search_bullets_target: 'input', action: 'keyup->search-bullets#update' }
  %>
  <%# <%= submit_tag "Search", class: "btn btn-flat" %>
<% end %>
